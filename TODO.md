# TODO
- [x] ตรวจสอบไฟล์ `platformio.ini` ปัจจุบัน
- [x] แก้ `platformio.ini` ให้เข้ากับ Arduino Uno R3 โดยเพิ่ม `default_envs = uno` และตั้ง `monitor_speed = 115200`
- [x] ยืนยันว่า PlatformIO ทำงานได้ด้วยคำสั่ง `pio run -e uno`
- [x] แก้ `src/main.cpp` ให้โปรแกรมเสถียรขึ้นด้วย debounce + อัปเดต LCD เฉพาะเมื่อสถานะเปลี่ยน
- [x] ปรับการอ่าน KY-021 ให้ใช้ขา D7 ตามโจทย์



